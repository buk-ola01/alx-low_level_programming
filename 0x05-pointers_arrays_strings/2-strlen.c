#include "main.h"
#include <string.h>
/**
 * _strlen - function that returns the length of a string.
 * @s: - pointer parameter
 * Return: length of string.
 */

int _strlen(char *s)
{
	int length;

	length = strlen(s);
	return (length);
}
